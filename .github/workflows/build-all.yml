name: Build all platforms
on:
  workflow_dispatch:

jobs:
  
  build-desktop:
    uses: ./.github/reusable-workflows/build-desktop.yml
    with:
      env: 'tz2'

  build-wasm:
    uses: ./.github/reusable-workflows/build-wasm.yml
    with:
      env: 'prod'
  
  custom-job:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: echo "custom job"